"""
X（Twitter）投稿文 自動生成
戦略：役立つ情報60% / 共感20% / 雑学10% / 商品紹介10%
構造：①興味を引く一文 → ②共感・問題提起 → ③解決方法 → ④まとめ
インプレ強化：バイラルフック + ハッシュタグ + エンゲージCTA
"""
import os
import random
import json
import re
import sys
from datetime import datetime, timedelta
from pathlib import Path

# gemini_client / db_client をプロジェクトルートから使用
sys.path.insert(0, str(Path(__file__).parent.parent))
from db_client import db

# A8キャッシュ操作は crawlers パッケージに委譲
from crawlers.crawler_a8 import (
    load_programs            as _a8_load_programs,
    weighted_choice          as _a8_weighted_choice_fn,
    increment_posted         as _a8_increment_posted_fn,
    select_for_post          as _a8_select_for_post,
    pop_from_cache           as _a8_pop_from_cache,
    increment_posted_history as _a8_increment_history,
    mark_as_posted           as _a8_mark_as_posted,
)


def _load_feedback_insights() -> dict:
    """
    analytics_feedback.py が保存した insights を DB から読み込む。
    DB 未設定・読み込みエラーは空 dict を返す（フォールバック動作）。
    """
    try:
        return db.get_insights()
    except Exception:
        return {}


def _build_feedback_context(insights: dict, platform: str = "x") -> str:
    """
    insights から X 投稿プロンプト用のフィードバックブロックを生成する。
    停滞フラグ・勝ちパターン・避けるべきパターンを含む。
    """
    lines = []
    platform_data = insights.get(platform, {})

    if platform_data.get("winning_patterns"):
        lines.append("【過去の勝ちパターン（必ず参考にする）】")
        for p in platform_data["winning_patterns"][:3]:
            lines.append(f"・{p}")

    if platform_data.get("avoid_patterns"):
        lines.append("【避けるべきパターン（絶対に使わない）】")
        for p in platform_data["avoid_patterns"][:2]:
            lines.append(f"・{p}")

    if insights.get("exploration_mode"):
        lines.append("【探索モード: 今回は全く新しい切り口で書く（過去パターンを踏襲しない）】")

    return "\n".join(lines)

# ─────────────────────────────────────────
# 投稿タイプの定義と重み
# ─────────────────────────────────────────
POST_TYPES = [
    {"type": "useful",   "label": "役立つ情報",       "weight": 25},
    {"type": "empathy",  "label": "共感・体験",       "weight": 15},
    {"type": "progress", "label": "収益進捗ログ",     "weight": 15},
    {"type": "product",  "label": "Amazon商品紹介",   "weight": 10},
    {"type": "trivia",   "label": "雑学・ネタ",       "weight": 10},
    {"type": "a8",       "label": "A8アフィリエイト", "weight": 15},
    {"type": "rakuten",  "label": "楽天商品紹介",     "weight": 10},
]

# ─────────────────────────────────────────
# 投稿時間帯（4スロット）
# ─────────────────────────────────────────
TIME_SLOTS = [
    (7, 9),
    (11, 13),
    (17, 19),
    (21, 23),
]

# ─────────────────────────────────────────
# ハッシュタグ（タイプ別 × 2〜3個）
# ─────────────────────────────────────────
HASHTAGS_BY_TYPE = {
    "useful":    ["#副業", "#AI活用", "#ライフハック"],
    "empathy":   ["#副業", "#フリーランス", "#副業初心者"],
    "trivia":    ["#AI", "#雑学", "#テクノロジー"],
    "product":   ["#副業", "#アフィリエイト", "#AI副業"],
    "rakuten":   ["#楽天市場", "#楽天アフィリエイト"],
    "a8":        ["#PR", "#副業"],
    "progress":  ["#副業", "#BuildInPublic", "#AI副業"],
}

# エンゲージメントCTA
ENGAGEMENT_CTAS = [
    "保存しておくと役立ちます",
    "役に立ったらRTしてもらえると嬉しいです",
    "同じ悩みの人に届いてほしい",
    "気になった人はフォローしてね",
    "詳しくはプロフィールのリンクから",
    "いいねしてくれると励みになります",
    "あとで見返せるように保存推奨",
    "共感したらシェアしてもらえると嬉しい",
]

# ─────────────────────────────────────────
# 投稿スタイル5種（ランダム選択で脱ボット化）
# ─────────────────────────────────────────
CTR_STYLES = {
    "共感・悩み解決": (
        "「〇〇で困っていませんか？」と問いかけるフックから始める。"
        "読者が抱える具体的な悩みを1行で言語化し、解決策として記事・情報を提示する。"
        "最後は「同じ悩みの人に届いてほしい」「参考になれば」など共感で締める。"
    ),
    "ベネフィット追求": (
        "ツールや手法を使うと「何時間節約できるか」「いくら得するか」を数字で見せる。"
        "抽象的な便利さではなく、具体的な数字・時間・金額に落とし込む。"
        "「使う前と後の差分」を短くまとめるのが効果的。"
    ),
    "逆説・意外性": (
        "「実は〇〇は間違いだった」「99%の人が知らない」「やってはいけない〇〇」など"
        "常識を覆す一言か意外な事実から始める。"
        "後半で正解・実態を明かし、読者に「知れてよかった」と思わせる。"
    ),
    "箇条書き・要約": (
        "3つのポイントでメリット・事実・手順を整理し、視認性を高める。"
        "各行を短く（10〜20字）まとめ、縦スクロールで読める構造にする。"
        "最後に一行でまとめの言葉を入れる。"
    ),
    "トレンド・速報": (
        "「今話題の」「2026年最新」「たった今わかった」などリアルタイム感のある言葉を使う。"
        "AIトレンド・市場の変化・新機能など鮮度の高い情報に乗せる。"
        "フォロワーが「今すぐ確認したい」と思う緊迫感を出す。"
    ),
}

# スタイル別キーワード候補（Geminiに渡すコンテキスト）
_STYLE_KEYWORDS_BY_TYPE = {
    "useful":   ["業務効率化", "AI副業", "副業の始め方", "時短ツール", "生産性向上"],
    "empathy":  ["副業 失敗", "続けられない", "最初の1万円", "稼げない理由"],
    "trivia":   ["AIの意外な事実", "お金の知識", "検索より速い方法", "副業の税金"],
    "product":  ["業務効率化ツール", "クラウド会計", "DXツール", "バックオフィス効率化"],
    "progress": ["AI副業 進捗", "自動化システム", "副業ログ"],
    "rakuten":  ["楽天 人気商品", "コスパ最強", "プチプラ美容"],
}

# バイラルフックパターン（Geminiプロンプトに渡すリファレンス）
VIRAL_HOOK_PATTERNS = [
    "〇〇した結果、△△になった（具体的な数字入り）",
    "「〇〇できない」と思ってた。でも実は××だった",
    "副業で稼げない人がやってしまう△△",
    "知らないと損する〇〇の話",
    "〇〇を3ヶ月続けたら△△円になった正直な話",
    "やってみて分かった。〇〇は××だった",
    "〇〇している人、実は△△だった",
    "1日△△分でできる〇〇",
    "99%の人が知らない〇〇の使い方",
    "〇〇を始めて△△ヶ月。正直に言います",
]

MAX_TWEET_UNITS = 275  # Twitter実測で280ちょうどが拒否されるため5単位の安全マージン


def x_char_count(text: str) -> int:
    """Xの文字数カウント（CJK・全角 = 2単位、ASCII = 1単位）"""
    count = 0
    for ch in text:
        cp = ord(ch)
        if (0x1100 <= cp <= 0x115F or
            0x2E80 <= cp <= 0x9FFF or
            0xA000 <= cp <= 0xA4CF or
            0xA960 <= cp <= 0xA97F or
            0xAC00 <= cp <= 0xD7FF or
            0xF900 <= cp <= 0xFAFF or
            0xFE10 <= cp <= 0xFE1F or
            0xFE30 <= cp <= 0xFE6F or
            0xFF00 <= cp <= 0xFF60 or
            0xFFE0 <= cp <= 0xFFE6 or
            0x1B000 <= cp <= 0x1B0FF or
            0x1F004 <= cp <= 0x1F0CF or
            0x1F200 <= cp <= 0x1F2FF or
            0x20000 <= cp <= 0x2A6DF or
            0x2A700 <= cp <= 0x2CEAF or
            0x2CEB0 <= cp <= 0x2EBEF or
            0x2F800 <= cp <= 0x2FA1F or
            0x30000 <= cp <= 0x3134F):
            count += 2
        else:
            count += 1
    return count


def _truncate_to_x_units(text: str, max_units: int) -> str:
    """X文字単位でmax_units以内に切り詰める（末尾の…1単位を含む）"""
    count = 0
    for i, ch in enumerate(text):
        units = 2 if x_char_count(ch) == 2 else 1
        # …(1単位)を追加した場合でも超えないか確認
        if count + units > max_units - 1:
            return text[:i] + "…"
        count += units
    return text


def append_hashtags(text: str, post_type: str) -> str:
    """投稿テキストにハッシュタグを追加（X換算280単位以内に収める）"""
    tags = HASHTAGS_BY_TYPE.get(post_type, HASHTAGS_BY_TYPE["useful"])
    selected = random.sample(tags, min(2, len(tags)))
    hashtag_str = " ".join(selected)

    full = f"{text}\n\n{hashtag_str}"
    if x_char_count(full) <= MAX_TWEET_UNITS:
        return full

    one_tag = f"{text}\n\n{selected[0]}"
    if x_char_count(one_tag) <= MAX_TWEET_UNITS:
        return one_tag

    suffix = f"\n\n{selected[0]}"
    max_text_units = MAX_TWEET_UNITS - x_char_count(suffix)
    return _truncate_to_x_units(text, max_text_units) + suffix


# ─────────────────────────────────────────
# テンプレート 100パターン
# useful: 25 / empathy: 20 / trivia: 20 / product: 25 / progress: 10
# ─────────────────────────────────────────
TEMPLATES = {
    "useful": [
        {
            "hook": "AI副業で月3万円なら、今すぐ始められます。",
            "empathy": "「副業って難しそう」と思ってる人へ。\n\n実際にやってみて分かったこと：",
            "solution": "・AI記事作成（1記事15分）\n・AI翻訳（スキル不要）\n・AI画像販売（無料で始める）\n\nどれも最初の1万円まで3ヶ月以内で行けた。",
            "summary": "詳しくはプロフィールリンクにまとめてます",
        },
        {
            "hook": "時間を2倍使えるようになった、たった1つの習慣。",
            "empathy": "「やることが多すぎて全部できない」\n\nそれ、順番の問題かもしれません。",
            "solution": "朝の最初の30分だけ、\n一番大事なこと1つだけに集中する。\n\nこれだけで1日の質が変わります。",
            "summary": "シンプルだけど、続けると効果がデカい",
        },
        {
            "hook": "スマホ1台でできる副業、3選。",
            "empathy": "「パソコンないから副業できない」\n\nそんなことないです。",
            "solution": "① ポイ活（月5,000円〜）\n② アンケートモニター\n③ AI画像販売\n\n全部スマホだけでOK。",
            "summary": "まず1つ試すだけで感覚つかめます",
        },
        {
            "hook": "ChatGPTを3ヶ月使ってみた。本当に仕事が速くなったのか正直に言います。",
            "empathy": "「AIって結局どう使えばいいの？」って最初は思ってた。",
            "solution": "実際に速くなったのはこの3つ：\n・メール文章（5分→30秒）\n・企画書のたたき台（1時間→10分）\n・調べもの（検索より答えが早い）\n\n苦手なのは「最新情報」と「数字の計算」。",
            "summary": "特性を理解してから使うと段違いに変わる",
        },
        {
            "hook": "副業で月1万円を最速で達成する方法、試してみた。",
            "empathy": "「副業収入の最初の1万円が一番難しい」\n\n本当にそう。最初は何をやっても0円が続く。",
            "solution": "一番早かったのはクラウドソーシング×AIの組み合わせ。\n\n・文章系の案件をChatGPTで下書き\n・修正して納品\n\n最初の1件で5,000円。2件で計1万円超えた。",
            "summary": "スキルより「始める」が大事",
        },
        {
            "hook": "無料で使えるAIツール、結局これだけあれば十分だった。",
            "empathy": "課金しまくって気づいた。\n\n無料でも90%の作業はできる。",
            "solution": "・文章作成 → ChatGPT(無料版) or Gemini\n・画像生成 → Canva AI\n・動画編集 → CapCut\n・スケジュール → Notion\n\n全部無料。",
            "summary": "まず無料で試してから課金を考えよう",
        },
        {
            "hook": "在宅ワークで月5万円稼ぐのに、特別なスキルは要りませんでした。",
            "empathy": "「スキルがないから副業できない」\n\nそれ、思い込みかもしれません。",
            "solution": "実際にやってみてわかったスキル不要の副業：\n\n・データ入力（地味だけど安定）\n・SNS運用代行（教えてもらえる）\n・テキスト校正（日本語が読めればOK）",
            "summary": "まずはやってみることが一番の近道",
        },
        {
            "hook": "Notionを使い始めて、タスクの忘れ物がゼロになった話。",
            "empathy": "「メモ帳やカレンダーが散らばって\nどこに何を書いたかわからない」\n\n以前の自分がこれでした。",
            "solution": "Notionに一本化してよかったこと：\n・全部1ヶ所で管理できる\n・テンプレートが使える\n・スマホとPCで同期される\n\n無料で始められます。",
            "summary": "情報の「置き場所」を決めるだけで楽になる",
        },
        {
            "hook": "節約より収入を増やすほうが、圧倒的に楽だった話。",
            "empathy": "節約を頑張っても\n月に数千円しか変わらなかった。",
            "solution": "それより副業で月1〜2万円増やす方が\n精神的にも楽で効果も大きかった。\n\n収入を増やす方向に思考を切り替えると\n選択肢が広がります。",
            "summary": "「節約」より「稼ぐ」に時間を使う",
        },
        {
            "hook": "副業を始めて3ヶ月で変わったこと、正直に全部言います。",
            "empathy": "最初の1ヶ月は本当に何も変わらなかった。",
            "solution": "変化が出てきたのは2〜3ヶ月目：\n\n・仕事の見え方が変わった\n・お金の流れが少しわかってきた\n・自分でコントロールできる感覚が出てきた\n\n収益より「変化」の方が先に来る。",
            "summary": "最初の3ヶ月は投資期間だと思うと楽になる",
        },
        {
            "hook": "Google検索より速くて精度が高い情報収集の方法。",
            "empathy": "「ネット検索すると広告ばかりで\n知りたい情報が出てこない」\n\nそれ、AIに聞けば解決します。",
            "solution": "Perplexity AIを使うと：\n・出典付きで回答してくれる\n・最新情報も拾える\n・広告なし\n\n無料で使えます。",
            "summary": "情報収集の効率が変わると、行動の質が変わる",
        },
        {
            "hook": "ブログを半年書き続けて、やっと収益が出てきた話。",
            "empathy": "最初の4ヶ月は0円でした。正直つらかった。",
            "solution": "変わったのは「キーワード戦略を変えた」から。\n\n競合が少ないキーワードに絞ったら\n5ヶ月目に初収益。6ヶ月目に月1万円超えた。\n\n継続×戦略が大事。",
            "summary": "方向性を変えるだけで結果が変わることがある",
        },
        {
            "hook": "1日10分でできる副業の仕組み作りを、3ヶ月続けた結果。",
            "empathy": "「時間がないから副業できない」\n\n時間がない人ほど、仕組みが必要です。",
            "solution": "やったこと：\n・記事生成をAIで自動化\n・SNS投稿をスケジュール予約\n・アフィリエイト申請だけ手動\n\n今は1日10分の確認だけで回ってます。",
            "summary": "最初に仕組みを作る時間を惜しまない",
        },
        {
            "hook": "フリーランスになる前に知っておきたかった3つのこと。",
            "empathy": "会社員のまま副業を始めて\n気づいたことがある。",
            "solution": "① 確定申告は思ったより簡単\n② 収入が不安定な月はメンタルがきつい\n③ 営業力より「専門性」の方が稼げる\n\n事前に知っておくと準備できます。",
            "summary": "知識があるだけでリスクが下がる",
        },
        {
            "hook": "AIライティングで月3万円達成した方法を全部公開します。",
            "empathy": "「ライターって専門家じゃないとできないんじゃ」\n\nそんなことないです。",
            "solution": "やったこと：\n・クラウドワークスで案件を探す\n・ChatGPTで下書き作成\n・自分の言葉で加筆修正\n・納品\n\n文章力より「構成力」と「修正力」が大事。",
            "summary": "AIはライバルじゃなくてパートナー",
        },
        {
            "hook": "副業で失敗した経験から学んだ、やってはいけないこと3選。",
            "empathy": "遠回りしてきたから言えることがある。",
            "solution": "① 最初から複数の副業を同時にやる\n② 稼げると聞いたものに飛びつく\n③ 継続できないことを選ぶ\n\n1つに絞って3ヶ月継続が最短ルートだった。",
            "summary": "失敗は前払いの授業料",
        },
        {
            "hook": "副業収入が安定してきたとき、最初に実感したこと。",
            "empathy": "お金じゃなくて、「選択肢が増えた」感覚でした。",
            "solution": "・嫌な仕事を断れるようになった\n・急な出費に怯えなくなった\n・将来の不安が少し軽くなった\n\n収入が増えることより\n「安心感」の方が先に来ます。",
            "summary": "副業の本当の価値は選択肢が増えること",
        },
        {
            "hook": "転職より副業の方が、リスクが低い理由。",
            "empathy": "「このまま会社にいていいのか」と悩んでいた頃の話。",
            "solution": "転職はリセット、副業は積み上げ。\n\n副業で月3万円稼げるスキルがあれば\nどこにいても生きていける安心感が出る。\n\n転職前に副業で実力を試す方が賢い。",
            "summary": "会社に依存しない力をつける",
        },
        {
            "hook": "お金の知識がないまま働くと、損する理由を具体的に説明します。",
            "empathy": "給料が上がっても生活が楽にならない人へ。",
            "solution": "知らないと損すること：\n・ふるさと納税（実質無料で返礼品）\n・iDeCo（所得税が減る）\n・医療費控除（10万円超えで還付）\n\n全部合計すると年数万円変わる。",
            "summary": "お金の勉強は一番ROIが高い投資",
        },
        {
            "hook": "週1時間だけ作業して月1万円を維持できている仕組みの話。",
            "empathy": "「忙しい社会人が副業で稼ぐのは無理」\n\nそれは仕組みを作ってないからかもしれません。",
            "solution": "仕組み化できたこと：\n・ブログ記事はAIが下書き（月4本自動生成）\n・SNS投稿はツールで自動化\n・アフィリエイトリンクは記事に仕込み済み\n\n初期投資（時間）さえかければ後は楽になる。",
            "summary": "一度作った仕組みは24時間働いてくれる",
        },
        {
            "hook": "Gemini APIを無料で最大限活用する設定と使い方。",
            "empathy": "「APIを使いたいけど課金が怖い」\n\n無料枠の範囲内でも結構使えます。",
            "solution": "無料で使える範囲：\n・1日あたりのリクエスト上限あり\n・gemini-2.0-flash-liteが最もコスパ良\n・バッチ処理なら1日のリクエストを節約可能\n\nAPIキーはGoogleアカウントで即取得できます。",
            "summary": "無料枠の設計を理解すると使い方が変わる",
        },
        {
            "hook": "副業を始める前に「やること」より「やらないこと」を決めた方がいい理由。",
            "empathy": "最初に全部やろうとして燃え尽きた経験があります。",
            "solution": "決めておくと楽になること：\n・稼げないことに時間を使わない\n・比較しすぎない\n・3ヶ月で成果が出なくても続ける\n\n取り組み方を決めると行動が安定する。",
            "summary": "ルールを作ると継続しやすくなる",
        },
        {
            "hook": "副業で稼ぐより先に、まずやるべきこと。",
            "empathy": "「何から始めればいいかわからない」\n\nその状態のまま始めても挫折します。",
            "solution": "先に決めておくこと：\n・何のために稼ぐのか（目的）\n・週に使える時間（リアルな見積もり）\n・最低3ヶ月は続ける覚悟\n\n目的が明確だと行動が続きます。",
            "summary": "始める前の準備が継続率を上げる",
        },
        {
            "hook": "ブログで稼ぐのに「文章力」はいらなかった話。",
            "empathy": "「文章が下手だからブログは無理」\n\n自分もそう思ってました。",
            "solution": "稼げる記事に必要なのは：\n・読者の悩みに答えること\n・キーワードを入れること\n・情報の正確さ\n\n文章の「上手さ」より「わかりやすさ」が大事。",
            "summary": "SEO記事は文学じゃない",
        },
        {
            "hook": "副業を3年続けて、本業の収入を超えた話。",
            "empathy": "最初は月0円で「向いてないのかも」と思った。",
            "solution": "収入の変化：\n1年目：月0〜1万円（試行錯誤）\n2年目：月3〜5万円（仕組み化）\n3年目：月10万円超（スケール）\n\n最初の1年が一番きつい。でもそこを超えると変わる。",
            "summary": "長期視点で見ると続けることが最強の戦略",
        },
    ],

    "empathy": [
        {
            "hook": "正直に言います。副業を始めた最初の1ヶ月は、1円も稼げませんでした。",
            "empathy": "やり方が分からなくて\n試行錯誤の毎日。",
            "solution": "でも続けてたら少しずつ形になってきた。\n\n最初はみんなそんなもん。",
            "summary": "「うまくいかない」は通過点",
        },
        {
            "hook": "「忙しくて副業する時間がない」←これ、1年前の自分です。",
            "empathy": "通勤中も帰宅後もヘトヘト。\nそんな状態で何ができるの、って。",
            "solution": "でも1日15分だけやってみたら\n3ヶ月で月1万円になってた。\n\n時間じゃなくて、やり方の問題だった。",
            "summary": "少しだけ試してみる価値はある",
        },
        {
            "hook": "副業始めた最初の月、全部失敗した。",
            "empathy": "ブログ→読まれない\nクラウドソーシング→案件取れない\nアフィリエイト→クリックゼロ\n\n全滅でした。",
            "solution": "それでも続けた理由は\n「やめたら確実に0円だから」。\n\n失敗し続けた2ヶ月目に\nやっと1件受注できた。",
            "summary": "続けることが最低限の必要条件",
        },
        {
            "hook": "会社の給料だけで生きていくことへの不安が、ある日限界になった。",
            "empathy": "昇給は年1回、数千円。\n物価はどんどん上がる。\n\n「このままでいいのか」という感覚。",
            "solution": "副業を始めたのはそこから。\n最初は月数百円だったけど\n1年続けたら生活費の1割くらいになった。\n\n全員に刺さる話じゃないけど\n動いたことは後悔してない。",
            "summary": "不安を行動に変える",
        },
        {
            "hook": "副業で初めてお金が入った日のことを今でも覚えてる。",
            "empathy": "金額は1,500円。\n\n小さい数字だけど\n自分の力で稼いだという実感は本物だった。",
            "solution": "その経験が次のモチベーションになった。\n\n最初の「小さな成功」が大事なのは\nそういう理由だと思う。",
            "summary": "最初の1円が一番価値がある",
        },
        {
            "hook": "副業を始めて気づいた、会社員でいることのリスク。",
            "empathy": "副業をやるまで\n「会社員は安定」だと思ってた。",
            "solution": "でも実際は：\n・給与の決定権は自分にない\n・仕事がなくなるリスクは常にある\n・スキルが会社専用になりがち\n\n副業は「保険」だった。",
            "summary": "リスク分散は早い方がいい",
        },
        {
            "hook": "「副業で稼いでいる人は才能があるだけ」←昔の自分が言ってたこと。",
            "empathy": "それ、嫉妬と言い訳だった。",
            "solution": "稼いでいる人と話してみると\n・特別な才能はなかった\n・ただ継続していた\n・失敗から学んでいた\n\n才能より行動量の話だった。",
            "summary": "才能の話にすると行動しなくていい言い訳になる",
        },
        {
            "hook": "副業をやめようと思った日が3回あった。",
            "empathy": "理由はいつも同じ。\n「成果が出ない」\n\n1回目：始めて2ヶ月目\n2回目：方向転換した直後\n3回目：比較して落ち込んだ時",
            "solution": "やめなかった理由：\n「やめたら確実にゼロになる」\n\nそれだけで続けた結果\n今は月3万円を超えた。",
            "summary": "やめないことが唯一の必勝法",
        },
        {
            "hook": "SNS投稿を毎日続けて100日目。変わったことを正直に言います。",
            "empathy": "最初の1ヶ月は\nいいねが1桁でした。",
            "solution": "100日後の変化：\n・フォロワーが少しずつ増えた\n・投稿のコツがつかめてきた\n・言葉にする習慣がついた\n\n数字より「習慣化できた」が一番の収穫。",
            "summary": "継続の効果は数字じゃないところから出てくる",
        },
        {
            "hook": "副業の収益が月1万円になった時、最初にやったこと。",
            "empathy": "使い道を考えた。\n\n答えは「副業の再投資」。",
            "solution": "・有料ツールの月額費用\n・書籍代\n・ドメイン代\n\n稼いだ分を全部次の仕組みに回した。\n\n今思うとこれが正解だった。",
            "summary": "初期は稼いだお金を仕組みに使う",
        },
        {
            "hook": "フリーランスに憧れてた頃と、実際にやってみた今の本音。",
            "empathy": "憧れ：自由な時間、在宅、稼げる\n現実：自己管理が大変、収入不安定、孤独",
            "solution": "でも「自分の力で稼ぐ経験」は\n会社員のままでは得られなかった。\n\n全部経験して、今は会社員×副業のハイブリッドが\n自分には合ってると感じてる。",
            "summary": "理想と現実のギャップを知った上で選ぶ",
        },
        {
            "hook": "副業で一番つらかったのは、お金じゃなくて「見えない努力」でした。",
            "empathy": "成果が出るまでの期間\n誰にも評価されない。\n\nそれが一番キツかった。",
            "solution": "だから記録をつけ始めた。\n・今日やったこと\n・何がうまくいったか\n・何がダメだったか\n\n自分で自分を評価する仕組みを作った。",
            "summary": "見えない努力を可視化すると続けやすくなる",
        },
        {
            "hook": "「副業は税金が複雑」で諦めそうになってた話。",
            "empathy": "確定申告が怖くて\n副業を始めるのを1年間ためらってた。",
            "solution": "実際やってみたら\nfreeeを使えば1〜2時間で完了した。\n\n怖かったのは「知らない」からだった。",
            "summary": "知識不足を行動しない言い訳にしない",
        },
        {
            "hook": "副業仲間ができたことで、続けられるようになった話。",
            "empathy": "一人でやってると\n孤独で挫折しやすい。",
            "solution": "SNSで同じ挑戦をしてる人を見つけて\n緩く繋がることにした。\n\n「頑張ってる人がいる」という事実だけで\nモチベーションが続く。",
            "summary": "環境を変えると行動量が変わる",
        },
        {
            "hook": "副業収入が本業の1割を超えた日に思ったこと。",
            "empathy": "「これ、意外とやれるじゃん」\n\n自分を過小評価してたことに気づいた。",
            "solution": "人間、やってみないと\n自分の可能性がわからない。\n\n副業をやって一番良かったのは\n「自分にもできる」という実感。",
            "summary": "自信は行動の後からついてくる",
        },
        {
            "hook": "副業で失敗しまくったからこそ言えること。",
            "empathy": "やってきた副業リスト（全部失敗）：\nアフィリエイト/DropShipping/NFT/FX\n\n全部中途半端に手を出して全部ダメだった。",
            "solution": "失敗から学んだのは\n「1つを深掘りする」こと。\n\nAIライティングに絞って3ヶ月\nやっと形になってきた。",
            "summary": "広く浅くより、狭く深く",
        },
        {
            "hook": "「副業で稼げる人」と「稼げない人」の差は才能じゃなかった。",
            "empathy": "分析してわかったこと。",
            "solution": "稼げる人の共通点：\n・行動が早い\n・失敗から学ぶ\n・小さく始める\n\n稼げない人の共通点：\n・完璧を目指す\n・情報収集だけで止まる\n・結果が出るまでの時間を見誤る",
            "summary": "違いは「始めたかどうか」だけ",
        },
        {
            "hook": "副業を始めたことで、本業への向き合い方が変わった話。",
            "empathy": "副業で稼ぐ難しさを知ってから\n会社の仕事のありがたさに気づいた。",
            "solution": "・安定した給料の価値がわかった\n・チームで働く強みが見えた\n・自分の得意/不得意がはっきりした\n\n副業は本業を客観的に見る鏡になった。",
            "summary": "副業は本業の「解像度」も上げてくれる",
        },
        {
            "hook": "副業を諦めかけた時に読み返す、自分へのメモ。",
            "empathy": "成果が出ない時はつらい。\nでも覚えておくこと：",
            "solution": "・最初の3ヶ月は誰でもほぼゼロ\n・続けた人だけが結果を見られる\n・今日の作業は必ず積み上がっている\n\n焦らない。今日も前に進んでいる。",
            "summary": "続けることが最大の戦略",
        },
        {
            "hook": "副業を始めて一番変わったのは、お金じゃなくて「考え方」だった。",
            "empathy": "稼ぎ方を考えるようになってから\n見える世界が変わった。",
            "solution": "・広告を見ても「仕組み」が気になる\n・ヒット商品に「なぜ売れるか」を考える\n・失敗しても「学び」として捉えられる\n\nお金より「思考の変化」が一番の収穫。",
            "summary": "副業は人生の解像度を上げる",
        },
    ],

    "trivia": [
        {
            "hook": "ChatGPTが1回返答するのに使う電力、知ってますか？",
            "empathy": "普通のGoogle検索の10倍以上らしい。",
            "solution": "それでも世界中で毎日何億回も使われてる。\n\nAIってどんだけエネルギー食ってんだ笑",
            "summary": "便利さとコストは常にトレードオフ",
        },
        {
            "hook": "AIに「ありがとう」って言う人、意外と多い説。",
            "empathy": "無意識に礼儀正しくなってしまう現象。",
            "solution": "感謝されるとAIの返答品質が上がる\nって研究もあるらしい。\n\n本当かどうかはともかく、",
            "summary": "優しい人はAIにも優しい説",
        },
        {
            "hook": "日本のサラリーマンの副業率、知ってますか？",
            "empathy": "実は思ったより少ない。",
            "solution": "副業している会社員は全体の約10%。\n\n裏を返すと、今副業してる人は\nまだ少数派。先行者優位がある。\n\n市場的には今がチャンス。",
            "summary": "少数派にいることは武器になる",
        },
        {
            "hook": "Gemini（Google AI）の「Flash」と「Pro」、何が違うか知ってますか？",
            "empathy": "API料金が全然違います。",
            "solution": "Flash：速い・安い・日常タスク向け\nPro：精度高い・高い・複雑な推論向け\n\n副業ブログ自動化なら\nFlashで十分すぎる。",
            "summary": "目的に合ったモデルを選ぶとコストが激減",
        },
        {
            "hook": "日本で一番検索されてる副業キーワード、何だと思いますか？",
            "empathy": "正解は「副業 バレない」らしい。",
            "solution": "副業そのものより「バレないか」の方が\n気になってる人が多いということ。\n\n実際は副業禁止の会社でも\n「届出制」が多く、禁止でないケースも多い。",
            "summary": "まず就業規則を確認してみよう",
        },
        {
            "hook": "「1万時間の法則」って本当に正しいのか、調べてみた。",
            "empathy": "「1万時間練習すれば誰でも一流になれる」\n\nこの話、半分嘘でした。",
            "solution": "正確には「質の高い意図的な練習」が条件。\n\nただこなすだけの1万時間では上達しない。\nフィードバックループが重要。",
            "summary": "時間より「やり方」の質が大事",
        },
        {
            "hook": "日本の物価上昇率、実は先進国の中でかなり低い話。",
            "empathy": "「インフレで生活が苦しい」と言われるけど",
            "solution": "欧米と比べると日本のインフレ率は\n相対的にまだ低い。\n\nただし賃金の上昇率がそれ以下なので\n実質的な生活は苦しくなってる。\n\n数字の見方で景色が変わります。",
            "summary": "統計は「何と比べるか」で意味が変わる",
        },
        {
            "hook": "SNSのアルゴリズムが「リンク付き投稿」のリーチを下げてる理由。",
            "empathy": "「投稿してもインプレが伸びない」\n\nリンクを入れてませんか？",
            "solution": "XもInstagramも\nユーザーを外部に出したくないから\nリンク付き投稿は優先度が下がる。\n\n対策：リンクはリプライに入れる。",
            "summary": "プラットフォームの仕様を理解すると変わる",
        },
        {
            "hook": "「複利」って投資だけじゃなくてスキルにも使えるって話。",
            "empathy": "お金の複利は有名だけど",
            "solution": "スキルの複利とは：\n・ライティング力 × SEO知識 = ブログ収益\n・SNS運用 × 副業知識 = コンサル案件\n\nスキルが組み合わさると\n価値が乗算になる。",
            "summary": "スキルの組み合わせで希少価値が上がる",
        },
        {
            "hook": "「AIに仕事を奪われる」論、実際のデータで確認してみた。",
            "empathy": "怖いのはわかる。でも事実を見よう。",
            "solution": "今なくなっているのは：\n・単純繰り返し作業\n・定型的なデータ処理\n\n今増えているのは：\n・AIを使いこなす仕事\n・クリエイティブな判断\n\n「AIを使う側」になれば話は変わる。",
            "summary": "脅威を理解してから対策できる",
        },
        {
            "hook": "ブログのSEO順位、実は「更新頻度」よりこれが重要だった。",
            "empathy": "「とにかく毎日更新しろ」は古い情報。",
            "solution": "今のGoogleが重視するのは：\n・E-E-A-T（経験・専門性・権威性・信頼性）\n・検索意図とのマッチ度\n・クリック後の離脱率\n\n質 > 量 の時代。",
            "summary": "毎日更新より月4本の濃い記事の方が伸びる",
        },
        {
            "hook": "人間の集中力が続く時間、平均何分か知ってますか？",
            "empathy": "答えは約20〜25分。",
            "solution": "「ポモドーロテクニック」が\nそのデータに基づいてる。\n\n25分集中→5分休憩を繰り返すと\n生産性が上がるという研究が複数ある。\n\n副業の作業にも使えます。",
            "summary": "科学的に証明された時間管理法を使う",
        },
        {
            "hook": "「節約」と「投資」、どっちがお金持ちへの近道かを数字で比べてみた。",
            "empathy": "感覚じゃなくて計算で考えよう。",
            "solution": "節約：月1万円削っても年12万円\n副業投資：月1万円を投資スキルに使うと\n3年後に月5万円のリターンになる可能性\n\n長期では「稼ぐ力を伸ばす」方が圧倒的。",
            "summary": "お金の使い道の優先順位を変える",
        },
        {
            "hook": "日本で一番利用者が多いクラウドソーシングサービス、知ってますか？",
            "empathy": "答えはランサーズとクラウドワークスが2強。",
            "solution": "登録無料で今すぐ始められる。\n\n最初の案件獲得のコツ：\n・単価を下げて実績を作る\n・プロフィールを丁寧に書く\n・応募コメントを書き分ける\n\n最初の1件が一番難しい。",
            "summary": "まず1件受注することにフォーカスする",
        },
        {
            "hook": "Xのアルゴリズムが「いいね」より「返信」を重視する理由。",
            "empathy": "「いいね数が多いのに伸びない投稿」はなぜ？",
            "solution": "Xは「会話の活発さ」を重視してる。\n\n・返信数 > いいね数\n・引用RT > RT\n\n会話が起きる投稿を意識すると\nリーチが広がりやすい。",
            "summary": "エンゲージメントの質がインプレを決める",
        },
        {
            "hook": "フリーランスの案件単価、会社員の時給と比べると何倍か。",
            "empathy": "感覚的には「フリーランスの方が稼げそう」だけど",
            "solution": "時給換算すると意外と変わらない場合もある。\n\nフリーランスが割高になる条件：\n・特定のスキルを持っている\n・営業ができる\n・リピート案件がある\n\n稼げる仕組みを作れるかが鍵。",
            "summary": "フリーランスで稼ぐには「スキル×営業力」が要る",
        },
        {
            "hook": "「バズった投稿」に共通する3つの構造。",
            "empathy": "バズは運ではなく再現性がある。",
            "solution": "構造：\n① 強烈なフック（数字・逆説・驚き）\n② 共感か有益な情報\n③ 行動を促すCTA\n\nこの3つを意識するだけで\n反応率が変わります。",
            "summary": "バズの構造を知ると投稿の質が上がる",
        },
        {
            "hook": "「お金持ち」と「高収入」は別の概念という話。",
            "empathy": "年収1000万でも貯金なし、という人は珍しくない。",
            "solution": "本物のお金持ちの特徴：\n・資産が収入を生む仕組みを持つ\n・支出が収入より少ない\n・時間と自由がある\n\n収入を増やすだけでなく\n資産を作る視点が必要。",
            "summary": "収入より「資産形成」を意識する",
        },
        {
            "hook": "生産性ツールを何十個も試した結果、結局使い続けたのは3つだった話。",
            "empathy": "ツール沼にハマってた時期があります。",
            "solution": "最終的に残ったもの：\n・Notion（思考整理・タスク管理）\n・Google Calendar（スケジュール）\n・Obsidian（知識メモ）\n\nシンプルにするほど続けやすい。",
            "summary": "ツールは少ない方が生産性が上がることもある",
        },
        {
            "hook": "副業税金の基本、会社にバレずに確定申告する方法。",
            "empathy": "「確定申告でバレる？」と心配してる人へ。",
            "solution": "住民税を「自分で納付（普通徴収）」に\n設定すれば会社にバレにくい。\n\n確定申告書の「住民税の徴収方法」の欄を\n「自分で納付」にチェックするだけ。",
            "summary": "知識があると選択肢が増える",
        },
    ],

    "product": [
        {
            "hook": "AIツール、全部試して分かったコスパ最強はこれだった。",
            "empathy": "有料サービスに課金しまくった時期がありまして。",
            "solution": "結論、最初は無料ツールで十分でした。\n\n特にGeminiは無料なのに精度が高くて驚いた。\nライフハック系の文章なら十分使える。",
            "summary": "詳しくはプロフィールのリンクから",
        },
        {
            "hook": "副業で稼げる人と稼げない人、3ヶ月観察して分かった差。",
            "empathy": "スキルの差より、情報の差が大きかった。",
            "solution": "稼げる人は「どこで稼ぐか」を知ってる。\nやみくもに作業してるわけじゃない。\n\n自分が参考にしたサービスはプロフィールに。",
            "summary": "情報格差を埋めるだけで変わります",
        },
        {
            "hook": "ブログで月3万円稼ぐのに使ったツール、全部公開します。",
            "empathy": "「何を使えばいいかわからない」\n\nそれが一番最初の壁だよね。",
            "solution": "使ったもの（全部無料から始められる）：\n・ブログ基盤：はてなブログ\n・SEO分析：Googleサーチコンソール\n・記事生成補助：Gemini API\n・画像：Canva",
            "summary": "ツールより「使い方」が大事だけど、まず揃えること",
        },
        {
            "hook": "クラウド会計ソフト、freeeとマネーフォワードを両方使った正直な感想。",
            "empathy": "どちらを選べばいいか迷ってる人へ。",
            "solution": "freee：UIが直感的。確定申告が本当に簡単。\nマネーフォワード：Excel連携が強い。法人向き。\n\n副業・個人事業主ならfreeeが使いやすかった。\n無料プランから試せます。",
            "summary": "まず無料で両方触ってみることをおすすめ",
        },
        {
            "hook": "Chatworkを会社に導入してみた結果、メール量が半分になった話。",
            "empathy": "「チャットツール、本当に必要？」\n\nと懐疑的だったけどやってみたら。",
            "solution": "変化：\n・緊急連絡がリアルタイムになった\n・メールのCC地獄が消えた\n・情報がチャンネルで整理された\n\n月額から始められる。無料プランあり。",
            "summary": "コミュニケーションコストが下がると全体の生産性が上がる",
        },
        {
            "hook": "バックオフィス業務を自動化して、月20時間を取り戻した話。",
            "empathy": "経理・請求書・勤怠管理に追われてた頃。",
            "solution": "入れてよかったツール：\n・クラウド会計ソフト（自動仕訳）\n・電子契約サービス（印鑑不要）\n・チャットツール（メール削減）\n\nどれも初月無料から試せる。",
            "summary": "自動化できる作業に時間を使わない",
        },
        {
            "hook": "副業でブログを始めるなら、最初に入れておくべきツール3選。",
            "empathy": "ゼロから始めた時、何を使えばいいか本当に迷った。",
            "solution": "① サーチコンソール（無料）→ SEO状況を把握\n② Gemini（無料）→ 記事の下書きに使える\n③ Canva（無料）→ アイキャッチ画像を作れる\n\n全部無料。まずこれだけ入れれば動ける。",
            "summary": "最初は無料ツールで十分",
        },
        {
            "hook": "在宅ワーク環境を整えたら、生産性が1.5倍になった件。",
            "empathy": "「机と椅子があればいい」と思ってた。",
            "solution": "変えてよかったもの：\n・モニター追加（画面2枚で作業効率アップ）\n・チェアマット（長時間座っても疲れにくい）\n・ノイズキャンセリングイヤホン（集中力UPが違う）\n\n一度揃えたら長期で使える。",
            "summary": "環境への投資はROIが高い",
        },
        {
            "hook": "確定申告を自分でやってみた。思ったより簡単だった話。",
            "empathy": "「確定申告って難しそう」\n\n怖いのは最初だけでした。",
            "solution": "freeeを使えば：\n・銀行口座と連携して自動仕訳\n・スマホから申告書が作れる\n・税務署に行かなくてもe-tax対応\n\n無料プランから試せます。",
            "summary": "難しそうと思ってることは、やってみると意外と簡単",
        },
        {
            "hook": "DXツールを導入して、残業が月10時間減った話。",
            "empathy": "残業の多くは「非効率な作業」から来てた。",
            "solution": "効果があったもの：\n・議事録 → AI文字起こし（1/10の時間）\n・報告書 → テンプレート化（30分→5分）\n・情報共有 → チャットツール化（メール廃止）\n\nどれも導入コストは低い。",
            "summary": "ツールを入れるより使い続けることが重要",
        },
        {
            "hook": "SaaS比較サイトの「おすすめ」に騙された話と、本当の選び方。",
            "empathy": "アフィリエイト目的の比較記事を読んで\n高いツールを契約してしまった。",
            "solution": "本当の選び方：\n・無料トライアルで必ず自分で触る\n・「誰が使うか」を先に決める\n・機能より「サポートの質」を重視\n\n高いツールが正解とは限らない。",
            "summary": "比較記事より「自分で試す」が最強",
        },
        {
            "hook": "副業初月に使ったツール代、全部で月500円でした。",
            "empathy": "「副業って初期費用かかるんじゃ」\n\nそんなことはなかった。",
            "solution": "使ったもの：\n・はてなブログ（無料）\n・Gemini API（無料枠）\n・Canva（無料）\n・独自ドメイン（年1,000円以下）\n\n月500円でスタートできた。",
            "summary": "まず低コストで始めて、稼いでから投資する",
        },
        {
            "hook": "中小企業がDXで最初にやるべきこと、専門家に聞いてみた。",
            "empathy": "「DXって大企業だけのもの」\n\nそんなことないです。",
            "solution": "中小企業のDX第一歩：\n① 紙の業務をデジタル化（クラウド化）\n② コミュニケーションをチャットに\n③ 会計をクラウド会計に\n\nどれも月数千円から始められる。",
            "summary": "DXは大きい投資より小さな改善の積み重ね",
        },
        {
            "hook": "Google WorkspaceとMicrosoft 365、副業なら結局どっちがいいか。",
            "empathy": "迷ってる人が多いので比較してみた。",
            "solution": "一人でやるなら：Google Workspace（無料）で十分。\nチームがある場合：Teams連携があるM365が便利。\n\n副業・個人事業主は\nGmailとDriveがあれば今日から使える。",
            "summary": "用途で選べば迷わなくなる",
        },
        {
            "hook": "月3万円の副業を実現するためのツールスタックを公開。",
            "empathy": "「どのツールを使えばいいか」\n\nよく聞かれるので全部出します。",
            "solution": "【記事生成】Gemini API\n【投稿管理】はてなブログ\n【SNS自動化】GitHub Actions\n【分析】Googleサーチコンソール\n\n全部無料。",
            "summary": "道具を揃えたら次は使い倒すこと",
        },
        {
            "hook": "Notionで副業管理を始めたら、タスクの漏れがゼロになった。",
            "empathy": "以前はメモがバラバラで\n何をやればいいかわからなくなることがあった。",
            "solution": "Notionで管理してること：\n・キーワードリスト（記事候補を常に100件管理）\n・収益トラッカー（月次で記録）\n・作業ログ（今日やったことを書く）\n\n全部1ヶ所にまとめると頭が整理される。",
            "summary": "管理の仕組みが行動の質を上げる",
        },
        {
            "hook": "在宅ワークの生産性を上げるために試したこと、効果があったものだけ言う。",
            "empathy": "色々試した結果、残ったのはこれだけ。",
            "solution": "効果があったもの：\n・作業前にTodoを3つだけ書く\n・ポモドーロ（25分集中×5分休憩）\n・夜に明日の最重要タスクを1つ決める\n\n逆に効果なかったのは、完璧なタスク管理ツール。",
            "summary": "シンプルな方法が一番続く",
        },
        {
            "hook": "freeeを使い始めて、経理に使う時間が月8時間から1時間になった。",
            "empathy": "「経理は専門家に頼まないと無理」\n\nと思ってた副業初期。",
            "solution": "freeeで変わったこと：\n・銀行口座と自動連携\n・レシートをスマホで撮るだけで仕訳\n・確定申告書類が自動作成\n\n個人事業主・副業なら無料プランで十分。",
            "summary": "経理の自動化は副業の必須投資",
        },
        {
            "hook": "「バックオフィス効率化ツール」を5つ試して、本当に使い続けたのは2つだけだった。",
            "empathy": "ツール迷子になってた時期の話。",
            "solution": "最終的に残ったもの：\n・クラウド会計（freee）：経理を自動化\n・チャットツール（Chatwork）：社内連絡の効率化\n\nこの2つだけで管理コストが大幅に下がった。",
            "summary": "少ないツールを使い倒す方が効果的",
        },
        {
            "hook": "英語が話せなくてもできる副業、AI翻訳ツールで月2万円稼いだ話。",
            "empathy": "「語学力がないから国際案件は無理」\n\nDeepLとChatGPTがあれば違う話になります。",
            "solution": "やったこと：\n・海外コンテンツをDeepLで翻訳\n・ChatGPTで自然な日本語に調整\n・クラウドソーシングで納品\n\n翻訳精度が上がってから単価も上がった。",
            "summary": "AIが得意なことを自分の武器にする",
        },
        {
            "hook": "電力会社を乗り換えただけで年1万円以上安くなった話。",
            "empathy": "「面倒くさそう」でずっと後回しにしてた。",
            "solution": "実際にやったら：\n・比較サイトで10分で候補を絞れた\n・申し込みはオンラインで完了\n・切り替えは自動でやってくれた\n\n手間15分で年1万円。費用対効果最強。",
            "summary": "「面倒くさい」の裏にお得が隠れてる",
        },
    ],

    "progress": [
        {
            "hook": "【収益ログ】AI自動投稿を動かして今月の記録。",
            "empathy": "正直、最初は全然成果が出なかった。",
            "solution": "記事数：積み上げ中\nSNS投稿：毎日自動稼働中\n\n数字はまだ小さいけど、仕組みは確実に育ってる。",
            "summary": "引き続きここで公開します",
        },
        {
            "hook": "AIで副業を自動化して◯日目。正直な途中経過を報告します。",
            "empathy": "「稼げてる？」ってよく聞かれるので答える。",
            "solution": "Gemini APIで記事自動生成 → はてなブログに投稿\nSNS自動投稿 × 5プラットフォーム\n\nまだ収益は育成中。でも仕組みは動いてる。",
            "summary": "次の報告もここで",
        },
        {
            "hook": "副業の自動化システムを作り始めて最初に気づいたこと。",
            "empathy": "「自動化すれば楽になる」\n\n半分本当で、半分違った。",
            "solution": "楽になったこと：\n・毎日の投稿作業がゼロ\n・記事生成が自動化された\n\nまだ手動なこと：\n・品質チェック\n・戦略の修正\n\n自動化は道具。判断は人間の仕事。",
            "summary": "自動化できることと、できないことを分ける",
        },
        {
            "hook": "アフィリエイトで最初の報酬が確定した日のこと。",
            "empathy": "小さい金額だったけど\nめちゃくちゃ嬉しかった。",
            "solution": "その1件が証明してくれたこと：\n「仕組みは動いている」\n\nあとはこれを積み重ねるだけ。\nスモールウィンが一番のモチベーション。",
            "summary": "最初の成功体験が全てを変える",
        },
        {
            "hook": "今月のSNS運用結果を公開します。数字は正直に言います。",
            "empathy": "良いことも悪いことも全部出します。",
            "solution": "今月の結果：\n・はてなブログ記事：自動生成で量産中\n・X投稿：毎日5回自動投稿\n・Bluesky：クロスポストで運用\n\n今は種まき期間。収益はこれから。",
            "summary": "記録することで次の改善点が見えてくる",
        },
        {
            "hook": "AIブログ自動化、失敗した設定と成功した設定を全部公開。",
            "empathy": "試行錯誤の記録です。",
            "solution": "失敗：毎日同じキーワードで記事生成 → 重複コンテンツ\n成功：検索需要のあるキーワードに絞って生成 → 少しずつ流入\n\nSEOキーワード選定が一番重要だった。",
            "summary": "失敗は次の成功のデータになる",
        },
        {
            "hook": "副業自動化の月次レビューをやってみた結果。",
            "empathy": "「自動化してるのに確認するの？」\n\nはい。そこが一番大事です。",
            "solution": "月次でチェックすること：\n・どの記事に流入があるか\n・どのSNS投稿がエンゲージしてるか\n・次月の方向性\n\n「自動化 × 定期改善」が最強の組み合わせ。",
            "summary": "仕組みは作って終わりじゃなく育てるもの",
        },
        {
            "hook": "収益がゼロの期間、続けられた理由を振り返る。",
            "empathy": "最初の2ヶ月は本当に0円だった。",
            "solution": "続けられた理由：\n・記録をつけていたから\n・小さな改善が積み上がってるのが見えたから\n・「やめたら確実に0」という論理\n\nお金じゃなくて「仕組みへの信頼」が支えになった。",
            "summary": "プロセスを信じて続けることが一番難しく、一番大事",
        },
        {
            "hook": "副業を自動化するために投資したもの・しなかったものを公開。",
            "empathy": "お金をかければいいわけじゃないことも学んだ。",
            "solution": "投資してよかったもの：\n・有料ドメイン（年1,000円以下）\n・Gemini API（月数百円）\n\n投資しなくてよかったもの：\n・高額な「副業塾」\n・不要な有料ツール\n\n最初は無料で最大限やり切ることが大事。",
            "summary": "ROIを常に意識して投資を決める",
        },
        {
            "hook": "副業の自動化システム、今の稼働状況を全部報告します。",
            "empathy": "透明性を持って記録します。",
            "solution": "現在稼働中：\n・はてなブログ自動投稿\n・X/Bluesky 毎日自動投稿\n・A8.net案件自動取得\n・Amazonアフィリ自動ローテーション\n\n収益は育成中。仕組みの精度を上げてる段階。",
            "summary": "仕組みは走らせながら改善する",
        },
    ],
}


def pick_post_type() -> dict:
    """重み付きランダムで投稿タイプを選択"""
    total = sum(p["weight"] for p in POST_TYPES)
    r = random.randint(1, total)
    cumulative = 0
    for pt in POST_TYPES:
        cumulative += pt["weight"]
        if r <= cumulative:
            return pt
    return POST_TYPES[0]


_used_templates: dict = {}  # 同じ日に同じテンプレを使わないよう管理


def _load_dedup_index():
    """content_selector から重複インデックスをロード（import失敗時は空セットを返す）"""
    try:
        from content_selector import load_recent_log, build_recent_index
        recent_log = load_recent_log()
        return build_recent_index(recent_log)
    except Exception:
        return set(), set()


def generate_with_template(post_type: str) -> str:
    """テンプレートから投稿文を生成（同日重複 + 過去14日間重複防止）"""
    templates = TEMPLATES.get(post_type, TEMPLATES["useful"])
    today = datetime.now().strftime("%Y%m%d")
    key = f"{today}_{post_type}"

    full_texts, hooks = _load_dedup_index()

    used = _used_templates.get(key, [])
    # 同日未使用 かつ 過去14日間未投稿のテンプレートに絞る
    available = [
        (i, t) for i, t in enumerate(templates)
        if i not in used
        and not _is_template_duplicate(t, full_texts, hooks)
    ]
    if not available:
        # 全部重複なら同日制限だけ外してフォールバック
        available = [
            (i, t) for i, t in enumerate(templates)
            if not _is_template_duplicate(t, full_texts, hooks)
        ]
    if not available:
        # それでも全部重複ならテンプレート全体から選ぶ（最終フォールバック）
        available = list(enumerate(templates))
        _used_templates[key] = []

    idx, t = random.choice(available)
    _used_templates.setdefault(key, []).append(idx)

    text = f"{t['hook']}\n\n{t['empathy']}\n\n{t['solution']}\n\n{t['summary']}"
    return append_hashtags(text, post_type)


def _is_template_duplicate(t: dict, full_texts: set, hooks: set) -> bool:
    """テンプレートの hook が過去の投稿と重複しているか判定"""
    hook = t.get("hook", "").strip()
    return hook in hooks


def _load_progress_stats() -> dict:
    """money_agent の進捗状態を DB から読み込む"""
    try:
        state = db.get_agent_state()
        return {
            "total_articles": state.get("total_articles", 0),
            "today_articles": state.get("today_articles", 0),
            "last_run":       str(state.get("last_run", ""))[:10],
        }
    except Exception:
        return {"total_articles": 0, "today_articles": 0, "last_run": ""}


def generate_with_gemini(post_type: str, label: str) -> str:
    """
    Gemini API で投稿文を生成（gemini_client 経由・バックオフ・キャッシュなし）。
    失敗時はテンプレートにフォールバック。
    """
    try:
        from money_agent.gemini_client import generate as gemini_generate
    except ImportError:
        return generate_with_template(post_type)

    # フィードバックinsights読み込み
    insights         = _load_feedback_insights()
    feedback_context = _build_feedback_context(insights, platform="x")
    temperature      = insights.get("last_temperature", 0.7)

    cta = random.choice(ENGAGEMENT_CTAS)

    # ── スタイルをランダム選択 ────────────────────────────────
    style_name, style_guide = random.choice(list(CTR_STYLES.items()))

    # ── キーワードをタイプ別に選択 ───────────────────────────
    kw_candidates = _STYLE_KEYWORDS_BY_TYPE.get(post_type, _STYLE_KEYWORDS_BY_TYPE["useful"])
    target_keyword = random.choice(kw_candidates)

    # ── progress タイプは実稼働データを注入 ─────────────────
    progress_context = ""
    if post_type == "progress":
        stats = _load_progress_stats()
        progress_context = f"""
【実際の稼働データ（これをそのまま使って投稿文を作る）】
・累計記事数: {stats['total_articles']}記事
・今日の記事数: {stats['today_articles']}記事
・最終実行日: {stats['last_run']}
・SNS自動投稿: X/Bluesky/はてな/note で毎日稼働中
"""

    type_instructions = {
        "useful":   "AI副業・時短・節約・生産性向上などに関する役立つ情報投稿",
        "empathy":  "副業や生活改善での失敗談・体験談・共感を呼ぶ投稿",
        "trivia":   "AI・テクノロジー・お金に関する意外な雑学・ネタ投稿",
        "product":  "AIツールや副業サービスを体験談・比較として自然に紹介する投稿（直接的な広告表現は禁止）",
        "progress": "AI副業の『現在進行形の検証ログ』。上記の実際の数字を使って、試行錯誤の過程をリアルに書く。",
    }

    prompt = f"""# 役割
あなたはSNSマーケティングとコピーライティングの専門家です。
X（Twitter）でクリック率（CTR）が高く、かつ「人間味」のある投稿文を1つ作成してください。

# 入力情報
- キーワード: {target_keyword}
- 投稿スタイル: {style_name}
- 投稿タイプ: {label}（{type_instructions.get(post_type, '')}）
{progress_context}
# 選択されたスタイルのガイドライン（必ず適用）
【{style_name}スタイル】
{style_guide}

{feedback_context}

# 制約条件（厳守）
- 文字数は100文字〜140文字以内（ハッシュタグ除く）
- 改行を活用して縦読みしやすくする
- 「いかがでしたか？」などの定型文は一切禁止
- 語尾・言い回しを毎回崩して自然な口調にする
- 広告・宣伝っぽい表現は使わない
- URLは不要（別で追加する）
- ハッシュタグは不要（別で追加する）
- 最後は「{cta}」で締める

# 構造
1行目：強烈なフック（{style_name}スタイルの書き出し）
↓（空行）
中間：共感・情報・体験談（箇条書きOK）
↓（空行）
最終行：{cta}

投稿文のみ出力してください。前置き・説明・タイトルは不要。"""

    # SNS投稿は毎回新鮮な内容にするためキャッシュなし・temperature動的適用
    text = gemini_generate(prompt, use_cache=False, temperature=temperature)
    if text:
        return append_hashtags(text, post_type)

    # フォールバック
    return generate_with_template(post_type)


def generate_post(force_type: str = None) -> dict:
    """投稿文を生成して返す"""
    post_type_info = pick_post_type() if not force_type else \
        next((p for p in POST_TYPES if p["type"] == force_type), POST_TYPES[0])

    post_type = post_type_info["type"]
    label     = post_type_info["label"]

    # 楽天商品紹介は専用関数で処理
    if post_type == "rakuten":
        result = generate_rakuten_product_post()
        if result:
            return result
        post_type = "useful"
        label = "役立つ情報"

    # A8アフィリエイトは専用関数で処理
    if post_type == "a8":
        result = generate_a8_program_post()
        if result:
            return result
        # キャッシュなし → product にフォールバック
        post_type = "product"
        label = "Amazon商品紹介"

    api_key = os.getenv("GEMINI_API_KEY")

    # 重複チェック用インデックスをロード
    full_texts, hooks = _load_dedup_index()

    # 重複していたら最大3回リトライ（Geminiの場合のみ）
    MAX_RETRY = 3
    text = None
    for attempt in range(MAX_RETRY):
        if api_key:
            candidate = generate_with_gemini(post_type, label)
        else:
            candidate = generate_with_template(post_type)

        hook_line = candidate.split("\n")[0].strip() if candidate else ""
        if candidate and candidate.strip() not in full_texts and hook_line not in hooks:
            text = candidate
            break
        if attempt < MAX_RETRY - 1:
            print(f"[dedup] 重複検知（{attempt + 1}回目）→ リトライ")

    # リトライ全部重複なら最後の候補をそのまま使う
    if not text:
        text = candidate if candidate else generate_with_template(post_type)

    return {
        "type":  post_type,
        "label": label,
        "text":  text,
        "chars": len(text),
    }


def generate_value_thread(post_type: str = "useful") -> dict:
    """
    スレッド形式投稿を生成する（脱ボット化）。

    tweet1: 価値コンテンツ（リンクなし）→ アルゴリズムによるリーチ最大化
    tweet2: アフィリエイトリンク or 詳細案内（リプライで投稿）

    Returns: {"tweet1": str, "tweet2": str}
    """
    try:
        from money_agent.gemini_client import generate as gemini_generate
    except ImportError:
        # フォールバック: 通常投稿だけ返す
        post = generate_post(force_type=post_type)
        return {"tweet1": post["text"], "tweet2": ""}

    cta = random.choice(ENGAGEMENT_CTAS)

    # スタイルとキーワードをランダム選択
    style_name, style_guide = random.choice(list(CTR_STYLES.items()))
    kw_candidates = _STYLE_KEYWORDS_BY_TYPE.get(post_type, _STYLE_KEYWORDS_BY_TYPE["useful"])
    target_keyword = random.choice(kw_candidates)

    prompt = f"""# 役割
あなたはSNSマーケティングとコピーライティングの専門家です。
X（Twitter）スレッドの1ツイート目（リンクなし・バズ狙い）を作成してください。

# 入力情報
- キーワード: {target_keyword}
- 投稿スタイル: {style_name}

# 選択されたスタイルのガイドライン（必ず適用）
【{style_name}スタイル】
{style_guide}

# 制約条件（厳守）
- リンクは一切入れない（リプライで誘導する）
- 文字数は100文字〜140文字以内（ハッシュタグ除く）
- 「いかがでしたか？」などの定型文は一切禁止
- 語尾・言い回しを毎回崩して自然な口調にする
- 広告・宣伝表現は禁止
- ハッシュタグは不要
- 最後は「{cta}」または「続きはリプライで」で締める

投稿文のみ出力してください。前置き・説明は不要。"""

    tweet1_text = gemini_generate(prompt, use_cache=False)
    if not tweet1_text:
        post = generate_post(force_type=post_type)
        tweet1_text = post["text"]

    tweet1 = append_hashtags(tweet1_text, post_type)

    # tweet2: 詳細/リンク案内（短め・自然なつなぎ）
    tweet2_templates = [
        "気になった方はプロフィールのリンクから詳細を確認できます。\n同じ悩みを持つ人に届いてほしい。",
        "具体的なやり方はプロフィールのブログにまとめました。\n役に立ったらリプライで教えてもらえると嬉しいです。",
        "詳しく知りたい方はプロフィールのリンクへどうぞ。\n質問があればリプライでどうぞ。",
        "プロフィールのリンクに関連記事をまとめています。\n同じことで悩んでいる人の参考になれば。",
    ]
    tweet2 = random.choice(tweet2_templates)

    return {"tweet1": tweet1, "tweet2": tweet2}


_AMAZON_PRODUCT_HISTORY_PATH = Path(__file__).parent / "product_history.json"
_AMAZON_COOLDOWN_DAYS = 14


def _load_amazon_product_history() -> list:
    try:
        data = json.loads(_AMAZON_PRODUCT_HISTORY_PATH.read_text(encoding="utf-8"))
        if isinstance(data, dict):
            return data.get("entries", [])
        if isinstance(data, list):
            return data
    except Exception:
        pass
    return []


def _save_amazon_product_history(entries: list) -> None:
    try:
        _AMAZON_PRODUCT_HISTORY_PATH.write_text(
            json.dumps({"entries": entries}, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
    except Exception as e:
        print(f"  [AmazonPost] product_history 保存失敗: {e}")


def _mark_amazon_product_posted(product: dict) -> None:
    """投稿完了後にproduct_history.jsonへ記録（14日クールダウン）"""
    key = product.get("asin") or product.get("search_keyword", "")
    if not key:
        return
    entries = _load_amazon_product_history()
    now = datetime.now().isoformat()
    for e in entries:
        if e.get("key") == key:
            e["last_posted_at"] = now
            break
    else:
        entries.append({
            "key":            key,
            "title":          product.get("title", ""),
            "last_posted_at": now,
        })
    # 古いエントリを削除（30日以上前）
    cutoff = (datetime.now() - timedelta(days=30)).isoformat()
    entries = [e for e in entries if (e.get("last_posted_at") or "") >= cutoff]
    _save_amazon_product_history(entries)
    print(f"  [AmazonPost] mark_posted: {product.get('title', key)[:40]}")


def _filter_amazon_cooldown(products: list) -> list:
    """last_posted_at が14日以内の商品をフィルタして返す"""
    cutoff = (datetime.now() - timedelta(days=_AMAZON_COOLDOWN_DAYS)).isoformat()
    history = {e["key"]: e.get("last_posted_at", "") for e in _load_amazon_product_history()}
    available = []
    for p in products:
        key = p.get("asin") or p.get("search_keyword", "")
        posted_at = history.get(key, "")
        if posted_at < cutoff:
            available.append(p)
    if available:
        return available
    # 全件クールダウン中 → 最も古く投稿されたものを優先して全件返す
    print("  [AmazonPost] 全商品クールダウン中 → 最古投稿を優先して全件返す")
    history_oldest = sorted(products, key=lambda p: history.get(
        p.get("asin") or p.get("search_keyword", ""), ""
    ))
    return history_oldest


def generate_amazon_product_post(force_refresh: bool = False) -> dict:
    """
    Amazonガジェット商品のスレッド投稿を生成して返す
    スレッド形式（tweet1/tweet2/tweet3）で返す
    """
    try:
        import sys
        sys.path.insert(0, os.path.dirname(__file__))
        from fetch_amazon_deals import fetch_deals
        from generate_amazon_thread import generate_thread

        products = fetch_deals("gadget", count=5, force_refresh=force_refresh)
        if not products:
            return {}

        # クールダウン済み商品を除外
        products = _filter_amazon_cooldown(products)

        # intent_score 上位2件から重み付きランダム選択（1位を優遇）
        top = products[:2]
        scores = [p.get("intent_score", 50) for p in top]
        product = random.choices(top, weights=scores, k=1)[0]

        # 投稿済みとして記録（14日クールダウン開始）
        _mark_amazon_product_posted(product)

        thread  = generate_thread(product)

        if not thread.get("tweet1"):
            return {}

        return {
            "type":    "amazon_thread",
            "label":   "Amazon商品紹介",
            "product": product,
            "thread":  thread,
            "text":    thread["tweet1"],
            "chars":   len(thread["tweet1"]),
        }
    except Exception as e:
        print(f"⚠️  Amazon商品投稿生成エラー: {e}")
        return {}


_RAKUTEN_HISTORY_PATH  = Path(__file__).parent / "rakuten_post_history.json"
_RAKUTEN_COOLDOWN_DAYS = 7


def _load_rakuten_history() -> list:
    try:
        data = json.loads(_RAKUTEN_HISTORY_PATH.read_text(encoding="utf-8"))
        return data if isinstance(data, list) else []
    except Exception:
        return []


def _save_rakuten_history(entries: list) -> None:
    try:
        _RAKUTEN_HISTORY_PATH.write_text(
            json.dumps(entries, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
    except Exception as e:
        print(f"  [Rakuten] rakuten_post_history 保存失敗: {e}")


def _mark_rakuten_posted(product: dict, category_name: str) -> None:
    """投稿完了後に履歴へ記録（7日クールダウン）"""
    key = product.get("url", product.get("name", ""))
    if not key:
        return
    entries = _load_rakuten_history()
    now = datetime.now().isoformat()
    for e in entries:
        if e.get("key") == key:
            e["last_posted_at"] = now
            break
    else:
        entries.append({
            "key":            key,
            "name":           product.get("name", "")[:40],
            "category":       category_name,
            "last_posted_at": now,
        })
    # 30日以上前のエントリを削除
    cutoff = (datetime.now() - timedelta(days=30)).isoformat()
    entries = [e for e in entries if (e.get("last_posted_at") or "") >= cutoff]
    _save_rakuten_history(entries)
    print(f"  [Rakuten] mark_posted: {product.get('name', key)[:40]}")


def _filter_rakuten_cooldown(products: list) -> list:
    """7日以内に投稿済みの商品を除外する"""
    cutoff = (datetime.now() - timedelta(days=_RAKUTEN_COOLDOWN_DAYS)).isoformat()
    history = {e["key"]: e.get("last_posted_at", "") for e in _load_rakuten_history()}
    available = [p for p in products if history.get(p.get("url", p.get("name", "")), "") < cutoff]
    if available:
        return available
    # 全件クールダウン中 → 最も古い投稿を先頭に並べ直して全件返す
    print("  [Rakuten] 全商品クールダウン中 → 最古投稿を優先")
    return sorted(products, key=lambda p: history.get(p.get("url", p.get("name", "")), ""))


def generate_rakuten_product_post() -> dict:
    """
    楽天市場の人気商品をX投稿用テキストに変換して返す。
    スレッド形式: tweet1（商品紹介）/ tweet2（アフィリエイトリンク）
    """
    try:
        import sys, os
        sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
        from money_agent.rakuten_product_article import fetch_rakuten_products, ARTICLE_CATEGORIES
        import random as _random

        # クールダウン済みカテゴリを除外（週内に同カテゴリが重複しないよう）
        hist_categories = {e.get("category") for e in _load_rakuten_history()
                           if (e.get("last_posted_at") or "") >=
                           (datetime.now() - timedelta(days=_RAKUTEN_COOLDOWN_DAYS)).isoformat()}
        available_cats = [c for c in ARTICLE_CATEGORIES if c["name"] not in hist_categories]
        if not available_cats:
            available_cats = ARTICLE_CATEGORIES  # 全カテゴリ消化済みならリセット

        category = _random.choice(available_cats)
        products = fetch_rakuten_products(category["genre_id"], hits=10)
        if not products:
            return {}

        # クールダウン済み商品を除外してランダム選択
        candidates = _filter_rakuten_cooldown(products[:5])
        product = _random.choice(candidates)
        _mark_rakuten_posted(product, category["name"])

        name    = product["name"][:30]
        price   = product["price"]
        reviews = product["review_count"]
        avg     = product["review_avg"]
        url     = product["url"]

        # Geminiで紹介文生成（なければテンプレ）
        api_key = os.getenv("GEMINI_API_KEY")
        if api_key:
            try:
                from money_agent.gemini_client import generate as gemini_generate
                prompt = f"""楽天市場の商品をXで紹介する投稿を作成してください。

商品名: {product['name'][:50]}
価格: {price}円
カテゴリ: {category['name']}
レビュー数: {reviews}件 / 評価: {avg}点

【ルール】
- 1ツイート目: 100文字以内。商品の魅力を体験談風に（「〜してみた」「〜だった」形式）
- アフィリエイト感を出さず、リアルな一言レビューとして書く
- 絵文字1〜2個まで
- ハッシュタグなし（別で付ける）
- JSON形式のみ: {{"tweet1": "..."}}"""
                result = gemini_generate(prompt, temperature=0.8)
                import json as _json, re as _re
                m = _re.search(r'\{.*\}', result or '', _re.DOTALL)
                tweet1 = _json.loads(m.group()).get("tweet1", "") if m else ""
            except Exception:
                tweet1 = ""
        else:
            tweet1 = ""

        if not tweet1:
            tweet1 = f"{category['name']}で{reviews}件レビューの人気商品🛒 {price}円でこのクオリティはコスパ◎"

        tweet2 = f"▶ {name}\n{url}\n\n#楽天市場 #{category['name'].replace('・', '')} #楽天アフィリエイト"

        return {
            "type":   "rakuten",
            "label":  "楽天商品紹介",
            "text":   tweet1,
            "thread": {"tweet1": tweet1, "tweet2": tweet2},
            "chars":  len(tweet1),
        }
    except Exception as e:
        print(f"⚠️  楽天商品投稿生成エラー: {e}")
        return {}


def _a8_weighted_choice(programs: list) -> dict:
    """crawlers.crawler_a8.weighted_choice に委譲"""
    return _a8_weighted_choice_fn(programs)


def _a8_increment_posted(ins_id: str) -> None:
    """crawlers.crawler_a8.increment_posted に委譲"""
    _a8_increment_posted_fn(ins_id)


def generate_a8_program_post() -> dict:
    """
    A8.net 承認済みプログラムの X投稿を生成する（永久機関版）。

    【選択ロジック】
    1. a8_programs_cache.json（キュー）に在庫あり
       → weighted_choice で1件選択 → 投稿後にキューから削除
    2. キューが空
       → a8_programs_history.json（全履歴）からランダム選択
       → キューが補充されるまで A8投稿が止まらない

    ・ハッシュタグ: 保存時に Gemini が抽出済み
    ・リンク: hatena_url 優先、なければ affiliate_url
    ・景品表示法対応: 必ず #PR を付与

    Returns: generate_post() と同じ形式 dict、失敗時は空 dict
    """
    program, source = _a8_select_for_post()
    if not program:
        return {}

    name          = program.get("name", "")
    reward        = program.get("reward", "")
    affiliate_url = program.get("affiliate_url", "")
    hatena_url    = program.get("hatena_url", "")
    # ジャンルハッシュタグ（Gemini 抽出済み）+ #PR
    genre_tags    = program.get("hashtags", ["#副業"])
    hashtag_str   = "#PR " + " ".join(genre_tags)

    if not affiliate_url:
        return {}

    # リンクは hatena 記事 URL を優先（スパム判定回避）
    link_url = hatena_url if hatena_url else affiliate_url

    # Bitly 短縮（利用可能なら）
    try:
        sys.path.insert(0, str(Path(__file__).parent.parent / "money_agent"))
        from affiliate_tracker import shorten_url
        link_url = shorten_url(link_url) or link_url
    except Exception:
        pass

    # ── Gemini で投稿本文生成 ────────────────────────────────
    tweet_body = ""
    api_key = os.getenv("GEMINI_API_KEY")
    if api_key:
        try:
            from money_agent.gemini_client import generate as gemini_generate
        except ImportError:
            gemini_generate = None

        if gemini_generate:
            style_name, style_guide = random.choice(list(CTR_STYLES.items()))
            cta = random.choice(ENGAGEMENT_CTAS)

            prompt = f"""# 役割
あなたはSNSマーケティングとコピーライティングの専門家です。
A8.netのアフィリエイトプログラムをX（Twitter）で自然に紹介する投稿文を1つ作成してください。

# プログラム情報
- サービス名: {name}
- 報酬・特徴: {reward}
- ジャンルキーワード: {' '.join(genre_tags)}

# 選択されたスタイル: {style_name}
{style_guide}

# 制約条件（厳守）
- 本文は 80〜110 文字以内（URL・ハッシュタグ除く）
- 広告感を抑えつつ「使ってみた」「試した」などリアルな体験談風に書く
- 「いかがでしたか？」などの定型文は禁止
- URL・ハッシュタグは不要（別で付加する）
- 最後は「{cta}」で締める

投稿本文のみ出力。前置き・説明は不要。"""

            tweet_body = (gemini_generate(prompt, use_cache=False) or "").strip()

    # ── フォールバック: テンプレート ────────────────────────
    if not tweet_body:
        templates = [
            f"{name}、使ってみたら思ったより良かった。\n\n{reward}のキャッシュバックもあって、登録して損はない感じ。\n\n気になる人はリンクから確認してみて。",
            f"副業仲間に教えてもらった{name}。\n\n報酬{reward}で、今申し込みで特典あり。\n\nコスパ良すぎて紹介したくなった。",
            f"{name}を先週から使い始めた。\n\n{reward}のアフィリエイト報酬付きで、紹介できるレベルのサービス。\n\n詳細は下のリンクへ。",
            f"正直、{name}は登録前は半信半疑だった。\n\n使ってみたら普通に便利で、{reward}もついてくる。\n\n迷ってるなら一度確認してみる価値はある。",
            f"今月おすすめしたいサービス：{name}。\n\n{reward}の特典があって、今が申し込みのタイミングかも。\n\n詳しくはリンクで。",
            f"{name}の存在、最近まで知らなかった。\n\n{reward}という条件がついていて、使って損はなかった。\n\n気になった方はどうぞ。",
            f"友人に「なんで使ってないの？」と言われた{name}。\n\n試したら確かに良くて、{reward}もついてくるとは。\n\n詳細リンクを置いておきます。",
            f"{name}、知名度の割に使ってる人が少ない気がする。\n\n{reward}という好条件つき。\n\n損得で考えたら登録一択だった。",
            f"節約・副業を考えてる人に聞いてほしい。\n\n{name}という選択肢、知ってましたか。\n\n{reward}の条件あり。詳細はリンクから。",
            f"やってみてわかった{name}の本当のメリット。\n\n{reward}というのは最初知らなかった。\n\nリンクで確認してみて。",
        ]
        tweet_body = random.choice(templates)

    # ── 全体テキスト組み立て ─────────────────────────────────
    full_text = f"{tweet_body}\n\n{link_url}\n{hashtag_str}"

    # 280単位オーバー時は本文を切り詰め
    if x_char_count(full_text) > MAX_TWEET_UNITS:
        suffix    = f"\n\n{link_url}\n{hashtag_str}"
        max_body  = MAX_TWEET_UNITS - x_char_count(suffix)
        tweet_body = _truncate_to_x_units(tweet_body, max_body)
        full_text  = f"{tweet_body}{suffix}"

    ins_id = program.get("ins_id", "")

    # キューから消費 + 履歴に last_posted_at を記録（30日クールダウン）
    if source == "cache":
        _a8_pop_from_cache(ins_id)   # キューから削除（消費）
    _a8_mark_as_posted(ins_id)       # 履歴の last_posted_at / posted_count を更新

    print(
        f"[A8Post] 生成完了: {name} "
        f"(source={source}, posted_count: {program.get('posted_count', 0)+1}) "
        f"タグ: {hashtag_str}"
    )
    return {
        "type":    "a8",
        "label":   "A8アフィリエイト",
        "text":    full_text,
        "chars":   len(full_text),
        "program": program,
        "source":  source,
        "thread":  {"tweet1": full_text, "tweet2": ""},
    }


def get_today_schedule() -> list:
    """今日の投稿スケジュール（4回分）を生成"""
    today = datetime.now().date()
    schedule = []
    for slot_start, slot_end in TIME_SLOTS:
        hour   = random.randint(slot_start, slot_end - 1)
        minute = random.randint(0, 59)
        schedule.append(datetime(today.year, today.month, today.day, hour, minute))
    schedule.sort()
    return schedule


def preview_posts(count: int = 4):
    """生成した投稿をプレビュー表示"""
    print("=" * 55)
    print("今日のX投稿プレビュー")
    print("=" * 55)
    schedule = get_today_schedule()
    for i in range(count):
        post = generate_post()
        post_time = schedule[i] if i < len(schedule) else None
        print(f"\n【投稿 {i+1}/4】{post['label']} ({post['chars']}文字)")
        if post_time:
            print(f"投稿予定時刻: {post_time.strftime('%H:%M')}")
        print("─" * 45)
        print(post["text"])
    print("=" * 55)


if __name__ == "__main__":
    env_path = Path(__file__).parent.parent / ".env"
    if env_path.exists():
        with open(env_path) as f:
            for line in f:
                line = line.strip()
                if line and not line.startswith("#") and "=" in line:
                    k, v = line.split("=", 1)
                    os.environ.setdefault(k.strip(), v.strip())
    preview_posts()
